Python Data Structures : Crafting Singly Linked List Essential Operations | bhavya sharma | Medium Open app Sign Sign Write Sign Sign Python Data Structures : Crafting Singly Linked List Essential Operations bhavya sharma Follow 4 min read · Nov 2 , 2024 -- Listen Share linked list linear data structure consisting sequence elements element points . Unlike arrays , linked lists allow dynamic memory allocation , means easily add remove elements resizing reallocating memory . article , explore implement singly linked list Python , including basic operations like insertion , deletion , displaying list , reversing . Understanding Structure Linked List diving implementation , let understand key components singly linked list : Node : fundamental building block linked list . node contains parts : Data : actual value stored node . : pointer references node sequence . 2 . LinkedList : overall structure manages nodes . maintains reference node ( head ) list . Step 1 : Creating Node Class step implementing linked list create Node class . class encapsulate data pointer node . class Node : def _ _ init__(self , data ): self.data = data   # Store value self.next =   # Initialize pointer Step 2 : Creating LinkedList Class , let implement LinkedList class , include methods insertion , deletion , displaying list , reversing . class LinkedList : def _ _ init__(self ): self.head =   # Initialize head list Step 3 : Insertion Method insertion method allows add new node beginning list . efficient require traversal list . def insert(self , data ): new_node = Node(data )   # Create new node new_node.next = self.head   # Point new node current head self.head = new_node   # Update head new node Step 4 : Deletion Method deletion method removes occurrence node containing specified value . value found , message displayed . def delete(self , key ): current = self.head   # Start head list previous =   # track previous node current current.data ! = key : previous = current   # node current = current.next   # Update current node current :   # key found print(f"Element { key } found . " ) return previous :   # node delete head self.head = current.next   # Update head node : previous.next = current.next   # Bypass node delete Step 5 : Display Method display method traverses list prints node data . def display(self ): current = self.head   # Start head current :   # Check list print("The list . " ) return current : print(current.data , end= " - > " )   # Print current node data current = current.next   # node print("None " )   # Indicate end list Step 6 : Reversing List reverse method changes direction pointers , effectively reversing list . def reverse(self ): previous =   # Initialize previous current = self.head   # Start head current : next_node = current.next   # Store node current.next = previous   # Reverse pointer previous = current   # previous current node current = next_node   # node self.head = previous   # Update head new list Complete Implementation complete code linked list implementation : class Node : def _ _ init__(self , data ): self.data = data self.next = class LinkedList : def _ _ init__(self ): self.head = def insert(self , data ): new_node = Node(data ) new_node.next = self.head self.head = new_node def delete(self , key ): current = self.head previous = current current.data ! = key : previous = current current = current.next current : print(f"Element { key } found . " ) return previous : self.head = current.next : previous.next = current.next def reverse(self ): previous = current = self.head current : next_node = current.next current.next = previous previous = current current = next_node self.head = previous def display(self ): current = self.head current : print("The list . " ) return current : print(current.data , end= " - > " ) current = current.next print("None " ) # Example usage _ _ _ _ = = " _ _ main _ _ " : linked_list = LinkedList ( ) linked_list.insert(10 ) linked_list.insert(20 ) linked_list.insert(30 ) print("Initial linked list : " ) linked_list.display ( ) linked_list.delete(20 ) print("Linked list deleting 20 : " ) linked_list.display ( ) linked_list.reverse ( ) print("Reversed linked list : " ) linked_list.display ( ) Example Usage example usage section demonstrates utilize linked list implementation . following operations performed : Insertion : Nodes values 10 , 20 , 30 added list . Display : list displayed current elements . Deletion : node value 20 removed list . Reversal : linked list reversed , new order displayed . Conclusion implemented basic singly linked list Python , covering essential operations insertion , deletion , reversing , displaying list . Linked lists provide flexible way manage data dynamically , making important structure algorithms applications . implementation serves foundation understanding complex data structures algorithms involving linked lists . Python Data Structures Algorithms Linked Lists -- -- Follow Written bhavya sharma 40 Followers · 2 Following Follow responses Help Status Careers Press Blog Privacy Rules Terms Text speech